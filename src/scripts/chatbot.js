/* ============================================
   INNOVATEK — AI Chatbot Logic (OpenRouter)
   ============================================ */

import { select } from './utils.js';

// Chatbot functionality disabled - API key removed for security
export function initChatbot() {
  // Chatbot is disabled
  return;
}
